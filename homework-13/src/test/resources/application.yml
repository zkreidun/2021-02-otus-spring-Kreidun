spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  shell :
    interactive :
      enabled : false
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: create-drop

    show-sql: false

logging:
  level:
    ROOT: ERROR