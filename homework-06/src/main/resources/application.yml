spring:
  datasource:
    url: jdbc:h2:mem:homework
    initialization-mode: always
  shell:
    interactive:
      enabled: true

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true