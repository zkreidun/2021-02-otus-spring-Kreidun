spring:
  datasource:
    url: jdbc:h2:mem:homework
    initialization-mode: always
  shell :
    interactive :
      enabled : false
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: false